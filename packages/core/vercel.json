{
    "version": 2,
    "env": {},
    "builds": [
        {
            "src": "./dist/src/main.js",
            "use": "@vercel/node",
            "config": { "includeFiles": ["dist/**"] }
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "dist/src/main.js"
        }
    ]
}
